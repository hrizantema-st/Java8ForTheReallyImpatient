package java8ex3;

import java.util.Comparator;

public class Task_9 {
	/*
	 * 9. Write a method lexicographicComparator(String... fieldNames) that
	 * yields a comparator that compares the given fields in the given order.
	 * For example, a lexicographicComparator("lastname", "firstname") takes two
	 * objects and, using reflection, gets the values of the lastname field. If
	 * they are different, return the difference, otherwise move on to the
	 * firstname field. If all fields match, return 0.
	 */

	public Comparator<String> lexicographicComparator(String... fieldNames) {
		return null;
	}
}
